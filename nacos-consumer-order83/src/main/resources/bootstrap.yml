spring:
  application:
    name: nacos-consumer-order83
  cloud:
    nacos:
      discovery:
        server-addr: 10.10.10.145:8848

server:
  port: 83

server-url:
  nacos-user-service: http://nacos-payment-provider